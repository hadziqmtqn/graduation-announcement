{
    admin off
    auto_https off
}

:80 {
    root * /var/www/html/public
    encode zstd gzip
    
    # PHP processing via fastcgi to PHP-FPM
    php_fastcgi 127.0.0.1:9000
    
    # Static files
    file_server
    
    # Logging
    log {
        output stdout
    }
}